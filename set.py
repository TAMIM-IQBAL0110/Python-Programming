#set is the collection of the unordered items
#Each element is the set must be unique & immutable
# null_set = set() and set = {} is empty dictionary
# in set we can't store list or dictionary

nums = {1,3.4,5,6,"hello","world","hello"}
print(nums)
print(type(nums))
print(len(nums))


